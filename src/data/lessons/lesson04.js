// 第4章：函数
export default {
  id: 5,
  title: '函数',
  category: '基础',
  difficulty: 'easy',
  description: '学习函数的定义、调用和作用域',
  content: `# 函数

## 什么是函数？

函数就像一个"小机器"，你给它输入（参数），它处理后给你输出（返回值）。

**比如：** 榨汁机就是一个函数，你放入水果（输入），它给你果汁（输出）。

**为什么需要函数？**
1. **避免重复** - 同样的代码不用写多次
2. **便于维护** - 修改一个地方，所有调用都生效
3. **代码清晰** - 把复杂的任务分解成小块

## 函数声明 - 传统方式

最基本的函数定义方式。

function greet(name) {
  return \`你好, \${name}\`;
}

console.log(greet("张三"));  // 输出：你好, 张三

**解释：**
- \`function\` - 关键字，表示这是一个函数
- \`greet\` - 函数名，可以自己起
- \`(name)\` - 参数，函数的输入
- \`return\` - 返回结果，函数的输出

## 箭头函数 - 现代写法（ES6+，推荐）

箭头函数是更简洁的函数写法，现代JavaScript推荐使用。

**基本语法：**

const add = (a, b) => {
  return a + b;
};

**超级简写（单行返回）：**

const multiply = (a, b) => a * b;  // 省略了return和花括号
const square = x => x * x;         // 只有一个参数，可以省略括号

console.log(multiply(4, 5));  // 20
console.log(square(6));        // 36

**什么时候用箭头函数？** 大部分情况都可以用，代码更简洁！

## 参数 - 函数的输入

参数就是传递给函数的数据。

**无参数：**

const sayHello = () => {
  console.log("你好！");
};

**一个参数：**

const double = x => x * 2;

**多个参数：**

const add = (a, b) => a + b;

**默认参数（ES6+）：**

如果调用时没有传参数，就使用默认值。

const greet = (name = "访客") => {
  return \`欢迎, \${name}\`;
};

console.log(greet());         // 输出：欢迎, 访客（使用默认值）
console.log(greet("李四"));   // 输出：欢迎, 李四（使用传入的值）

**用途：** 避免参数为空时出错。

## 返回值 - 函数的输出

return 用来返回函数的结果。

const isEven = num => num % 2 === 0;  // 判断是否为偶数

console.log(isEven(4));  // true
console.log(isEven(7));  // false

**重要：** 
- 函数执行到 return 就结束了，后面的代码不会执行
- 如果没有 return，函数返回 undefined

## 作用域 - 变量的"活动范围"

作用域决定了变量在哪里可以被访问。

**全局变量 vs 局部变量：**

const globalVar = "我是全局变量";  // 在函数外，到处都能用

const test = () => {
  const localVar = "我是局部变量";  // 在函数内，只能在函数里用
  
  console.log(globalVar);  // ✅ 可以访问全局变量
  console.log(localVar);   // ✅ 可以访问局部变量
};

test();
console.log(globalVar);  // ✅ 可以访问全局变量
// console.log(localVar);  // ❌ 错误！函数外无法访问局部变量

**记住：** 函数内部可以看到外面的变量，但外面看不到函数内部的变量。

就像：你在房间里可以看到外面的风景，但外面的人看不到你房间里的东西。

## 💡 函数使用技巧

1. **函数名要有意义** - 看名字就知道做什么
   - ✅ \`calculateTotal\`
   - ❌ \`func1\`

2. **一个函数只做一件事** - 不要让函数太复杂

3. **优先使用箭头函数** - 代码更简洁

4. **给参数设置默认值** - 避免出错`,
  starterCode: `// ===== 函数练习（ES6+） =====

// 1. 箭头函数（ES6+，推荐）
const greet = name => \`你好, \${name}\`;

console.log(greet("张三"));  // 输出: 你好, 张三
console.log(greet("李四"));  // 输出: 你好, 李四

// 2. 带返回值的箭头函数
const add = (a, b) => a + b;  // 单行返回，省略return
const multiply = (a, b) => a * b;

console.log("\\n5 + 3 =", add(5, 3));        // 输出: 8
console.log("4 × 5 =", multiply(4, 5));      // 输出: 20

// 3. 默认参数
const greetWithDefault = (name = "访客") => {
  return \`欢迎, \${name}!\`;
};

console.log("\\n" + greetWithDefault());         // 输出: 欢迎, 访客!
console.log(greetWithDefault("王五"));           // 输出: 欢迎, 王五!

// 4. 多行函数体
const calculateArea = (width, height) => {
  const area = width * height;
  return \`面积是: \${area} 平方米\`;
};

console.log("\\n" + calculateArea(5, 3));  // 输出: 面积是: 15 平方米

// 5. 函数作为参数（高阶函数）
const applyOperation = (x, y, operation) => {
  return operation(x, y);
};

console.log("\\n使用add函数:", applyOperation(10, 5, add));           // 15
console.log("使用multiply函数:", applyOperation(10, 5, multiply));    // 50

// 6. 作用域示例
const globalMessage = "我是全局变量";

const testScope = () => {
  const localMessage = "我是局部变量";
  console.log("\\n函数内部:");
  console.log("  可以访问全局:", globalMessage);
  console.log("  可以访问局部:", localMessage);
};

testScope();
console.log("\\n函数外部:");
console.log("  可以访问全局:", globalMessage);
// console.log("  不能访问局部:", localMessage);  // 这行会报错

// 💡 练习：创建一个判断奇偶数的函数
// 💡 挑战：创建一个计算阶乘的递归函数`,
  solution: `// 函数练习答案

// 1. 判断奇偶数
const isEven = num => num % 2 === 0;
const isOdd = num => num % 2 !== 0;

console.log("4是偶数吗?", isEven(4));  // true
console.log("7是奇数吗?", isOdd(7));   // true

// 2. 找出两个数中的最大值
const max = (a, b) => a > b ? a : b;

console.log("\\nmax(10, 20):", max(10, 20));  // 20

// 3. 判断是否为正数
const isPositive = num => num > 0;

console.log("\\nisPositive(-5):", isPositive(-5));  // false
console.log("isPositive(5):", isPositive(5));       // true

// 4. 计算阶乘（递归）
const factorial = n => {
  if (n <= 1) return 1;
  return n * factorial(n - 1);
};

console.log("\\nfactorial(5):", factorial(5));  // 120

// 5. 数组求和
const sumArray = arr => {
  let result = 0;
  for (let num of arr) {
    result += num;
  }
  return result;
};

console.log("\\nsumArray([1,2,3,4,5]):", sumArray([1,2,3,4,5]));  // 15

// 测试
console.log("\\n=== 测试结果 ===");
console.log("max(10, 20):", max(10, 20));
console.log("isPositive(-5):", isPositive(-5));
console.log("isPositive(5):", isPositive(5));
console.log("factorial(5):", factorial(5));`,
};
