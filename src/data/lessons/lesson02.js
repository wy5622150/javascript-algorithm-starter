// 第2章：条件判断
export default {
  id: 3,
  title: '条件判断',
  category: '基础',
  difficulty: 'easy',
  description: '学习if-else条件语句和switch语句',
  content: `# 条件判断

## 什么是条件判断？

生活中我们经常需要做判断：如果下雨就带伞，如果不下雨就不带。编程也一样！

条件判断就是让程序根据不同的情况，做出不同的决定。

## if 语句 - 最基本的判断

if 的意思是"如果"。语法很简单：

if (条件) {
  // 如果条件成立，就执行这里的代码
}

**实际例子：判断是否成年**

const age = 18;
if (age >= 18) {
  console.log("你是成年人，可以投票了！");
}

**解释：** 如果年龄大于等于18，就输出这句话。如果年龄小于18，什么都不做。

## if-else 语句 - 二选一

有时候我们需要"如果...否则..."的判断。

if (条件) {
  // 条件成立时执行
} else {
  // 条件不成立时执行
}

**实际例子：判断考试是否及格**

const score = 75;
if (score >= 60) {
  console.log("恭喜你，考试及格了！🎉");
} else {
  console.log("很遗憾，没有及格，继续努力！💪");
}

**解释：** 分数大于等于60就及格，否则不及格。程序会走其中一条路。

## if-else if-else - 多个选择

当有多个条件需要判断时，可以用 else if（否则如果）。

**实际例子：评定成绩等级**

const grade = 85;
let level;

if (grade >= 90) {
  level = "优秀";        // 90分以上
} else if (grade >= 80) {
  level = "良好";        // 80-89分
} else if (grade >= 60) {
  level = "及格";        // 60-79分
} else {
  level = "不及格";      // 60分以下
}

console.log(\`你的成绩是\${grade}分，等级：\${level}\`);

**重要：** 程序会从上往下检查，一旦找到第一个成立的条件，就执行对应的代码，然后跳出。

**例如：** 85分会匹配到"良好"，不会继续往下判断了。

## 三元运算符 - 简写形式

当判断很简单时，可以用三元运算符简写。

格式：条件 ? 成立时的值 : 不成立时的值

const age = 20;
const status = age >= 18 ? "成年人" : "未成年人";
console.log(status);  // 输出：成年人

**什么时候用？** 只有简单的二选一判断时使用，太复杂就不要用了，会难以阅读。

## switch 语句 - 多个固定值的判断

当需要判断一个变量等于多个固定值时，switch 比 if-else 更清晰。

**实际例子：根据数字显示星期几**

const day = 3;
let dayName;

switch (day) {
  case 1:
    dayName = "星期一";
    break;  // 重要！找到匹配后要跳出
  case 2:
    dayName = "星期二";
    break;
  case 3:
    dayName = "星期三";  // day是3，匹配这里
    break;
  case 4:
    dayName = "星期四";
    break;
  case 5:
    dayName = "星期五";
    break;
  default:
    dayName = "周末";    // 都不匹配时执行
}

console.log(\`今天是\${dayName}\`);

**⚠️ 重要提醒：** 每个 case 后面必须加 break，否则会继续执行下一个 case（这叫"穿透"）。

**什么时候用switch？** 当你要判断一个变量等于很多个具体的值时（比如：1、2、3、4...），用switch更清晰。`,
  starterCode: `// ===== 条件判断练习（ES6+） =====

// 1. 简单的if语句
const age = 20;
if (age >= 18) {
  console.log("你已经成年了");  // 输出: 你已经成年了
}

// 2. if-else语句
const score = 75;
if (score >= 60) {
  console.log("考试及格！");    // 输出: 考试及格！
} else {
  console.log("考试不及格");
}

// 3. 多条件判断（成绩等级）- 使用const
const grade = 85;
let level;  // 需要重新赋值，用let

if (grade >= 90) {
  level = "优秀";
} else if (grade >= 80) {
  level = "良好";              // grade是85，会执行这里
} else if (grade >= 60) {
  level = "及格";
} else {
  level = "不及格";
}

console.log(\`\\n成绩: \${grade}, 等级: \${level}\`);  // 输出: 成绩: 85, 等级: 良好

// 4. 三元运算符（简写）
const temperature = 28;
const weather = temperature > 25 ? "炎热" : "凉爽";
console.log(\`天气: \${weather}\`);  // 输出: 天气: 炎热

// 5. switch语句
const day = 3;
let dayName;  // 需要重新赋值，用let

switch (day) {
  case 1:
    dayName = "星期一";
    break;
  case 2:
    dayName = "星期二";
    break;
  case 3:
    dayName = "星期三";  // day是3，会执行这里
    break;
  default:
    dayName = "其他";
}

console.log(\`今天是: \${dayName}\`);  // 输出: 今天是: 星期三

// 💡 练习：修改score、grade、day等变量的值，观察不同的输出
// 💡 挑战：使用箭头函数编写一个判断奇偶数的函数
`,
  solution: `let score = 85;
let grade;

if (score >= 90) {
  grade = "A";
} else if (score >= 80) {
  grade = "B";
} else if (score >= 70) {
  grade = "C";
} else if (score >= 60) {
  grade = "D";
} else {
  grade = "F";
}

console.log("分数:", score);
console.log("等级:", grade);`,
};
