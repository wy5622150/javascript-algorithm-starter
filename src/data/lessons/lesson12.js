// 练习3：传值与传址练习
export default {
  id: 12,
  title: '传值与传址练习',
  category: '练习',
  difficulty: 'medium',
  description: '通过实际练习深入理解原始类型和引用类型的区别',
  isExercise: true,
  introduction: `# 传值与传址练习题

## 📝 练习说明

这些练习题将帮助你深入理解JavaScript中传值与传址的区别，避免常见的数据错误。

**学习建议：**
1. 理解原始类型（传值）和引用类型（传址）的本质区别
2. 掌握正确复制对象和数组的方法
3. 注意函数参数传递时的陷阱
4. 先预测输出结果，再运行验证`,
  exercises: [
    {
      id: 1,
      title: '练习1：原始类型的传值 ⭐',
      description: `**要求：** 创建两个变量，验证原始类型的传值特性。

**任务：**
1. 创建变量 \`a = 10\`
2. 将 \`a\` 赋值给 \`b\`
3. 修改 \`b = 20\`
4. 输出 \`a\` 和 \`b\` 的值

**预期输出：**
\`\`\`
a = 10
b = 20
\`\`\`

**思考：** 为什么修改 \`b\` 不会影响 \`a\`？`,
      starterCode: `// 练习1：原始类型的传值
console.log("=== 练习1：原始类型的传值 ===");

// 在这里写代码
let a = 10;
let b = a;
// 修改 b 的值

console.log("a =", a);
console.log("b =", b);`,
      solution: `// 练习1：原始类型的传值 - 标准答案
console.log("=== 练习1：原始类型的传值 ===");

let a = 10;
let b = a;  // b 得到 a 的副本（复制值）
b = 20;     // 修改 b 不会影响 a

console.log("a =", a);  // 10
console.log("b =", b);  // 20

// 解释：原始类型传值，就像复印文件，修改复印件不影响原件`
    },
    {
      id: 2,
      title: '练习2：对象的传址陷阱 ⭐⭐',
      description: `**要求：** 演示对象的传址特性，理解为什么会出现"意外修改"。

**任务：**
1. 创建对象 \`person1 = { name: "张三", age: 25 }\`
2. 将 \`person1\` 赋值给 \`person2\`
3. 修改 \`person2.name = "李四"\`
4. 输出两个对象，观察结果

**预期输出：**
\`\`\`
person1: { name: "李四", age: 25 }
person2: { name: "李四", age: 25 }
\`\`\`

**思考：** 为什么修改 \`person2\` 会影响 \`person1\`？`,
      starterCode: `// 练习2：对象的传址陷阱
console.log("=== 练习2：对象的传址陷阱 ===");

// 在这里写代码
const person1 = { name: "张三", age: 25 };
const person2 = person1;  // 这里发生了什么？
// 修改 person2.name

console.log("person1:", person1);
console.log("person2:", person2);`,
      solution: `// 练习2：对象的传址陷阱 - 标准答案
console.log("=== 练习2：对象的传址陷阱 ===");

const person1 = { name: "张三", age: 25 };
const person2 = person1;  // person2 和 person1 指向同一个对象！
person2.name = "李四";    // 修改 person2 会影响 person1

console.log("person1:", person1);  // { name: "李四", age: 25 }
console.log("person2:", person2);  // { name: "李四", age: 25 }

// 解释：引用类型传址，就像两把钥匙开同一个房间
// person1 和 person2 指向内存中的同一个对象`
    },
    {
      id: 3,
      title: '练习3：正确复制对象 ⭐⭐',
      description: `**要求：** 使用展开运算符正确复制对象，避免相互影响。

**任务：**
1. 创建对象 \`original = { name: "王五", score: 90 }\`
2. 使用展开运算符 \`{ ...original }\` 创建副本 \`copy\`
3. 修改 \`copy.score = 100\`
4. 输出两个对象，验证互不影响

**预期输出：**
\`\`\`
original: { name: "王五", score: 90 }
copy: { name: "王五", score: 100 }
\`\`\`

**提示：** 使用 \`const copy = { ...original };\``,
      starterCode: `// 练习3：正确复制对象
console.log("=== 练习3：正确复制对象 ===");

const original = { name: "王五", score: 90 };
// 在这里使用展开运算符创建副本

// 修改 copy.score

console.log("original:", original);
console.log("copy:", copy);`,
      solution: `// 练习3：正确复制对象 - 标准答案
console.log("=== 练习3：正确复制对象 ===");

const original = { name: "王五", score: 90 };
const copy = { ...original };  // 使用展开运算符创建新对象
copy.score = 100;              // 修改副本不影响原对象

console.log("original:", original);  // { name: "王五", score: 90 }
console.log("copy:", copy);          // { name: "王五", score: 100 }

// 解释：展开运算符 {...} 会创建一个新对象
// 把原对象的属性复制到新对象中`
    },
    {
      id: 4,
      title: '练习4：数组的传址与复制 ⭐⭐',
      description: `**要求：** 对比数组的直接赋值和展开运算符复制的区别。

**任务：**
1. 创建数组 \`arr1 = [1, 2, 3]\`
2. 直接赋值：\`arr2 = arr1\`，然后 \`arr2.push(4)\`
3. 展开复制：\`arr3 = [...arr1]\`，然后 \`arr3.push(5)\`
4. 输出三个数组，观察区别

**预期输出：**
\`\`\`
arr1: [1, 2, 3, 4]
arr2: [1, 2, 3, 4]
arr3: [1, 2, 3, 4, 5]
\`\`\`

**思考：** 为什么 \`arr1\` 和 \`arr2\` 相同，但 \`arr3\` 不同？`,
      starterCode: `// 练习4：数组的传址与复制
console.log("=== 练习4：数组的传址与复制 ===");

const arr1 = [1, 2, 3];

// 方式1：直接赋值
const arr2 = arr1;
arr2.push(4);

// 方式2：展开运算符
// 在这里创建 arr3 并添加元素

console.log("arr1:", arr1);
console.log("arr2:", arr2);
console.log("arr3:", arr3);`,
      solution: `// 练习4：数组的传址与复制 - 标准答案
console.log("=== 练习4：数组的传址与复制 ===");

const arr1 = [1, 2, 3];

// 方式1：直接赋值（传址）
const arr2 = arr1;  // arr2 和 arr1 指向同一个数组
arr2.push(4);       // 修改 arr2 会影响 arr1

// 方式2：展开运算符（创建新数组）
const arr3 = [...arr1];  // arr3 是新数组
arr3.push(5);            // 修改 arr3 不影响 arr1

console.log("arr1:", arr1);  // [1, 2, 3, 4]
console.log("arr2:", arr2);  // [1, 2, 3, 4]（和 arr1 相同）
console.log("arr3:", arr3);  // [1, 2, 3, 4, 5]（独立的数组）

// 解释：
// - arr2 = arr1 只是复制了地址，指向同一个数组
// - [...arr1] 创建了一个新数组，内容相同但地址不同`
    },
    {
      id: 5,
      title: '练习5：函数参数的传递 ⭐⭐⭐',
      description: `**要求：** 创建两个函数，演示原始类型和引用类型作为参数的不同表现。

**任务：**
1. 创建函数 \`changeNumber(n)\`，尝试修改参数 \`n\`
2. 创建函数 \`changeArray(arr)\`，向数组添加元素
3. 测试两个函数，观察外部变量是否被改变

**预期输出：**
\`\`\`
修改前 num: 10
修改后 num: 10
修改前 myArray: [1, 2, 3]
修改后 myArray: [1, 2, 3, 999]
\`\`\`

**思考：** 为什么数字没变，但数组变了？`,
      starterCode: `// 练习5：函数参数的传递
console.log("=== 练习5：函数参数的传递 ===");

// 函数1：尝试修改数字参数
const changeNumber = n => {
  n = 999;
  console.log("函数内 n:", n);
};

// 函数2：修改数组参数
const changeArray = arr => {
  // 在这里向数组添加元素 999
};

// 测试
let num = 10;
let myArray = [1, 2, 3];

console.log("修改前 num:", num);
changeNumber(num);
console.log("修改后 num:", num);

console.log("\\n修改前 myArray:", myArray);
changeArray(myArray);
console.log("修改后 myArray:", myArray);`,
      solution: `// 练习5：函数参数的传递 - 标准答案
console.log("=== 练习5：函数参数的传递 ===");

// 函数1：尝试修改数字参数（原始类型）
const changeNumber = n => {
  n = 999;  // 只修改了副本，不影响外部
  console.log("函数内 n:", n);
};

// 函数2：修改数组参数（引用类型）
const changeArray = arr => {
  arr.push(999);  // 修改的是原数组！
};

// 测试
let num = 10;
let myArray = [1, 2, 3];

console.log("修改前 num:", num);
changeNumber(num);
console.log("修改后 num:", num);  // 10（未改变）

console.log("\\n修改前 myArray:", myArray);
changeArray(myArray);
console.log("修改后 myArray:", myArray);  // [1, 2, 3, 999]（被改变了！）

// 解释：
// - 原始类型作为参数：传递的是值的副本，函数内修改不影响外部
// - 引用类型作为参数：传递的是地址，函数内修改会影响外部

// 安全做法：如果不想修改原数组，先复制
const safeChangeArray = arr => {
  const newArr = [...arr];  // 先复制
  newArr.push(999);
  return newArr;  // 返回新数组
};`
    },
    {
      id: 6,
      title: '挑战：深拷贝嵌套对象 ⭐⭐⭐',
      description: `**要求：** 理解浅拷贝的局限性，使用深拷贝处理嵌套对象。

**任务：**
1. 创建嵌套对象 \`user = { name: "张三", address: { city: "北京" } }\`
2. 使用展开运算符浅拷贝：\`user2 = { ...user }\`
3. 修改 \`user2.address.city = "上海"\`，观察 \`user\` 是否被影响
4. 使用 \`JSON.parse(JSON.stringify())\` 深拷贝：\`user3\`
5. 修改 \`user3.address.city = "广州"\`，验证不影响原对象

**预期输出：**
\`\`\`
浅拷贝后:
user: { name: "张三", address: { city: "上海" } }
user2: { name: "张三", address: { city: "上海" } }

深拷贝后:
user: { name: "张三", address: { city: "上海" } }
user3: { name: "张三", address: { city: "广州" } }
\`\`\`

**提示：** 展开运算符只能浅拷贝一层`,
      starterCode: `// 挑战：深拷贝嵌套对象
console.log("=== 挑战：深拷贝嵌套对象 ===");

const user = { 
  name: "张三", 
  address: { city: "北京" } 
};

// 浅拷贝
const user2 = { ...user };
user2.address.city = "上海";

console.log("浅拷贝后:");
console.log("user:", user);
console.log("user2:", user2);

// 深拷贝
// 在这里使用 JSON.parse(JSON.stringify()) 创建 user3

console.log("\\n深拷贝后:");
console.log("user:", user);
console.log("user3:", user3);`,
      solution: `// 挑战：深拷贝嵌套对象 - 标准答案
console.log("=== 挑战：深拷贝嵌套对象 ===");

const user = { 
  name: "张三", 
  address: { city: "北京" } 
};

// 浅拷贝：只复制第一层
const user2 = { ...user };
user2.address.city = "上海";  // 嵌套对象还是共享的！

console.log("浅拷贝后:");
console.log("user:", user);    // address.city 也变成了"上海"
console.log("user2:", user2);

// 深拷贝：递归复制所有层级
const user3 = JSON.parse(JSON.stringify(user));
user3.address.city = "广州";  // 完全独立，不影响原对象

console.log("\\n深拷贝后:");
console.log("user:", user);    // address.city 还是"上海"
console.log("user3:", user3);  // address.city 是"广州"

// 解释：
// 浅拷贝：{ ...obj } 只复制第一层属性
//   - 第一层的原始类型值会被复制
//   - 第一层的引用类型还是指向原对象
// 深拷贝：JSON.parse(JSON.stringify(obj)) 递归复制所有层级
//   - 完全独立的新对象
//   - 注意：无法复制函数、undefined、Symbol等特殊值

// 更好的深拷贝方法（使用递归）
const deepClone = obj => {
  if (obj === null || typeof obj !== 'object') return obj;
  const clone = Array.isArray(obj) ? [] : {};
  for (let key in obj) {
    clone[key] = deepClone(obj[key]);
  }
  return clone;
};`
    }
  ]
};
